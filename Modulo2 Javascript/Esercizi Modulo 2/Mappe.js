// ciclo per la lunghezza della stringa
// identificare ogni carattere
// verificare se si ripete e calcolare quante volte

let stringa = "~~~ お前はもう死んでいる - e' la frase da utilizzare! ~~~"
let occorrenze = {}
let carattere = stringa[i]
for (index=0; index<stringa.length; index++) {
   carattere = stringa[i]++
}
occorrenze []