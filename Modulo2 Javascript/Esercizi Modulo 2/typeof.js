// leggendo online la documentazione dell'operatore `typeof` scrivere del codice che data una variabile `x` sia in grado di stampare `"x e' una stringa"` se per esempio `x` contiene una stringa, e rispettivamente per numeri e booleani

typeof Number
typeof String
let x = ""
if (x === "") {
    console.log(String)
}
else {
    console.log(Number)
}